bash script to get space on Ubuntu Server. It will return, percent of space available. 

If will do a comparison and if the space on server is less then it will send email. 

Configure mail before sending.
